services:
  haproxy:
    image: haproxy:2.9-alpine
    container_name: baedalsodae-haproxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    restart: unless-stopped